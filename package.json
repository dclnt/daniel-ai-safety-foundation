{
  "name": "weekly-progress-logger",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 1: weekly MasteryMaven -> progress-journal facts extraction (runs in GitHub Actions).",
  "scripts": {
    "extract": "tsx scripts/extract-weekly-progress.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "luxon": "^3.5.0"
  },
  "devDependencies": {
    "@types/luxon": "^3.4.2",
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
